version: 0.2

phases:
  pre_build:
    commands:
      - cd server
      - npm install
  build:
    commands:
      - npm run build

artifacts:
  files:
    - appspec.yml
  base-directory: server/build
